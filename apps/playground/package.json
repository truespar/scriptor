{
  "name": "@scriptor/playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Interactive demo consuming @truespar/scriptor-core (open + render + edit .docx, read/edit modes).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@truespar/scriptor-core": "workspace:*",
    "@truespar/scriptor-ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.2.2",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.6.0"
  }
}
